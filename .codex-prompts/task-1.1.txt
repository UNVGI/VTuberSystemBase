Execute only this single task (1.1 `rac-movin-mocap-factory` パッケージのディレクトリと `package.json` を新規作成する) according to the spec documents in .kiro/specs/rac-movin-mocap-factory/ (requirements.md, design.md, tasks.md). Before starting, output the task name. After completing the task, do NOT run UnityTestRunner (Unity is busy with other work) — only verify code-level correctness. If file changes exist, run git add for the specific files you created/modified, then commit with message "1.1 rac-movin-mocap-factory パッケージのディレクトリと package.json を新規作成する" and a body summarizing what was done. Use git commit -m "title" -m "body" multi-line form. tasks.md is a user-managed file and must not be modified by codex (the orchestrator handles checkbox toggling). Finally, output only OK or FAIL on the last line.

Important Unity .meta GUID rule (per global CLAUDE.md): use random 32-hex GUIDs from PowerShell [guid]::NewGuid().ToString('N') — never sequential or shifted patterns like a1b2c3d4...
