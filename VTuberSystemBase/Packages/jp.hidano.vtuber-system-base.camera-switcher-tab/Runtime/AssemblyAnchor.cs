#nullable enable

namespace VTuberSystemBase.CameraSwitcherTab
{
    /// <summary>
    /// Empty anchor type so the Runtime asmdef always has at least one source file.
    /// Concrete runtime types (Coordinator, OscStreamController, Adapters, Views) are
    /// added in subsequent tasks of the implementation plan.
    /// </summary>
    internal static class AssemblyAnchor
    {
    }
}
