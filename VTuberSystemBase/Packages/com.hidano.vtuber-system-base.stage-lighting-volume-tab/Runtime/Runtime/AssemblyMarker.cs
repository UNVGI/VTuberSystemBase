namespace VTuberSystemBase.StageLightingVolumeTab
{
    /// <summary>
    /// Internal marker so the Runtime assembly always has at least one type to compile,
    /// even before the rest of the implementation lands. Removed once a real type from
    /// this namespace ships (any subsequent task delivering Bootstrap, Services, View,
    /// etc. supersedes this).
    /// </summary>
    internal static class AssemblyMarker
    {
    }
}
